//https://codeforces.com/contest/31/problem/B
import java.util.Scanner;
import java.lang.Math; 
// import java.util.Arrays;
import java.util.Arrays;
import java.util.HashMap;
import java.util.Map;
import java.util.Map.Entry;
import java.util.IntSummaryStatistics;
 
public class Demo {
         
  public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        String a = scanner.nextLine();
        String words[] = a.split("");
        String word[] = a.split("@");
        
        int u = 0; 
        int er = 0;
        if(word.length-1 == 0 ){
                er++;
        }
        // System.out.println(word.length-1);
        if(words.length <= 2){
                er++;
        }

        String text = "";
        String q ="";
        int d = 0;
        for(int i = 0; i < a.length(); i++){
                if(u == 0){
                        if(words[i].equals("@")){
                                er++;
                                
                        }else {
                                text = text + words[i] ;
                                d = 1;
                                u=1;
                        }
                }else if(u == 1 && words[i].equals("@")){
                                  text = text + words[i];
                                  u=2;
                                  d = 0;
                                  
                }else if(u == 2){
                        if(words[i].equals("@")){
                               er++;
                        } else {
                              
                                  text = text+words[i] +"!";
                                 
                                 
                               u = 0;
                        }
                } else if(u == 1){
                          
                          text = text + words[i];
                }
               
        } 
             
              String new_text = "";
                 words = text.split("!");
                //  System.out.print(d);
                 for(int o = 0 ; o < words.length; o ++){
                        if(words.length-1 == o){
                                 new_text = new_text + words[o];
                        }else if(words.length-(1+d) == o){
                                 new_text = new_text + words[o];
                        } else {
                                 new_text = new_text + words[o]+",";
                        }
                 }
                if(er == 0){
                      
                               System.out.print(new_text);
                       
               } else {
                       System.out.print("No solution");
               }
}
}
